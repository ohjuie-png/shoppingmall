shopping-demo/
│── index.html<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <title>쇼핑몰 데모</title>
  <link rel="stylesheet" href="css/style.css">
</head>
<body>
  <header>
    <h1>나의 쇼핑몰</h1>
    <nav>
      <a href="index.html">홈</a>
      <a href="products.html">상품목록</a>
      <a href="cart.html">장바구니</a>
    </nav>
  </header>

  <main>
    <section class="hero">
      <h2>환영합니다 👕</h2>
      <p>간단한 데모 쇼핑몰 사이트입니다.</p>
      <a href="products.html" class="btn">상품 보러가기</a>
    </section>
  </main>

  <footer>
    <p>© 2025 My Shop Demo</p>
  </footer>
</body>
</html>

│── products.html<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <title>상품 목록</title>
  <link rel="stylesheet" href="css/style.css">
</head>
<body>
  <h1>상품 목록</h1>
  <div id="product-list" class="product-list"></div>

  <script src="js/products.js"></script>
  <script src="js/script.js"></script>
  <script>
    // 상품 목록 출력
    const productList = document.getElementById("product-list");
    products.forEach(product => {
      const item = document.createElement("div");
      item.classList.add("product-item");
      item.innerHTML = `
        <img src="${product.image}" alt="${product.name}">
        <h3>${product.name}</h3>
        <p>${product.price.toLocaleString()}원</p>
        <button onclick="addToCart(${product.id})">장바구니 담기</button>
        <a href="product.html?id=${product.id}">상세보기</a>
      `;
      productList.appendChild(item);
    });
  </script>
</body>
</html>

│── cart.html<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <title>장바구니</title>
  <link rel="stylesheet" href="css/style.css">
</head>
<body>
  <header>
    <h1>장바구니</h1>
    <nav>
      <a href="index.html">홈</a>
      <a href="products.html">상품목록</a>
      <a href="cart.html">장바구니</a>
    </nav>
  </header>

  <main>
    <h2>내 장바구니</h2>
    <div id="cart-items" class="cart-list"></div>
    <button onclick="clearCart()" class="btn danger">장바구니 비우기</button>
  </main>

  <footer>
    <p>© 2025 My Shop Demo</p>
  </footer>

  <script src="js/products.js"></script>
  <script src="js/script.js"></script>
  <script>
    renderCart();

    function clearCart() {
      localStorage.removeItem("cart");
      renderCart();
      alert("장바구니가 비워졌습니다.");
    }
  </script>
</body>
</html>

│── product.html<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <title>상품 상세</title>
  <link rel="stylesheet" href="css/style.css">
</head>
<body>
  <header>
    <h1>상품 상세</h1>
    <nav>
      <a href="index.html">홈</a>
      <a href="products.html">상품목록</a>
      <a href="cart.html">장바구니</a>
    </nav>
  </header>

  <main>
    <div id="product-detail" class="product-detail"></div>
  </main>

  <footer>
    <p>© 2025 My Shop Demo</p>
  </footer>

  <script src="js/products.js"></script>
  <script src="js/script.js"></script>
  <script>
    // URL에서 id 가져오기
    const params = new URLSearchParams(window.location.search);
    const productId = parseInt(params.get("id"));

    const product = products.find(p => p.id === productId);
    const detailContainer = document.getElementById("product-detail");

    if (product) {
      detailContainer.innerHTML = `
        <img src="${product.image}" alt="${product.name}">
        <h2>${product.name}</h2>
        <p class="price">${product.price.toLocaleString()}원</p>
        <p>${product.description}</p>
        <button onclick="addToCart(${product.id})" class="btn">장바구니 담기</button>
      `;
    } else {
      detailContainer.innerHTML = "<p>상품을 찾을 수 없습니다.</p>";
    }
  </script>
</body>
</html>

│
│── /css/
│     └── style.cssbody {
  font-family: Arial, sans-serif;
  margin: 0;
  padding: 0;
}

header, footer {
  background: #333;
  color: white;
  text-align: center;
  padding: 1rem;
}

nav a {
  margin: 0 10px;
  color: white;
  text-decoration: none;
}

.hero {
  text-align: center;
  padding: 2rem;
}

.btn {
  display: inline-block;
  padding: 10px 20px;
  background: #0078ff;
  color: white;
  border-radius: 5px;
  text-decoration: none;
}

.btn.danger {
  background: red;
}

.product-list, .cart-list {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(180px, 1fr));
  gap: 1rem;
  padding: 1rem;
}

.product-item, .cart-item {
  border: 1px solid #ddd;
  padding: 1rem;
  text-align: center;
}

.product-item img, .cart-item img {
  max-width: 100%;
  height: auto;
}

│
│── /js/
│     ├── script.js// script.js
function getCart() {
  return JSON.parse(localStorage.getItem("cart")) || [];
}

function saveCart(cart) {
  localStorage.setItem("cart", JSON.stringify(cart));
}

function addToCart(productId) {
  const cart = getCart();
  const product = products.find(p => p.id === productId);
  cart.push(product);
  saveCart(cart);
  alert(`${product.name}이(가) 장바구니에 담겼습니다.`);
}

function renderCart() {
  const cart = getCart();
  const cartContainer = document.getElementById("cart-items");
  if (!cartContainer) return;
  
  cartContainer.innerHTML = "";
  cart.forEach((item, index) => {
    const div = document.createElement("div");
    div.classList.add("cart-item");
    div.innerHTML = `
      <img src="${item.image}" alt="${item.name}">
      <p>${item.name} - ${item.price.toLocaleString()}원</p>
      <button onclick="removeFromCart(${index})">삭제</button>
    `;
    cartContainer.appendChild(div);
  });
}

function removeFromCart(index) {
  const cart = getCart();
  cart.splice(index, 1);
  saveCart(cart);
  renderCart();
}

│     └── products.js
const products = [
  {
    id: 1,
    name: "화이트 티셔츠",
    price: 19900,
    image: "images/tshirt-white.jpg",
    description: "편안하고 깔끔한 디자인의 화이트 티셔츠."
  },
  {
    id: 2,
    name: "블랙 후드티",
    price: 39900,
    image: "images/hoodie-black.jpg",
    description: "캐주얼하면서도 멋스러운 블랙 후드티."
  },
  {
    id: 3,
    name: "청바지",
    price: 49900,
    image: "images/jeans.jpg",
    description: "어떤 코디에도 잘 어울리는 기본 청바지."
  }
];

│
│── /images/
